# Grafana Dashboard

Import `dashboard.json` into Grafana to visualize Freebox metrics:

- Connection status & bandwidth
- Network traffic (download/upload)
- System temperatures
- FTTH optical power levels
- WiFi access points
- LAN hosts status

The dashboard uses a Prometheus datasource variable for portability.
